{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "additional formatting",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "node-sass scss/style.scss > ../css/dashboard.css -watch",
    "build": "node-sass scss/style.scss > ../css/dashboard.css --output-style compressed -x"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.14.1"
  }
}
